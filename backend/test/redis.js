const chai = require('chai')
const expect = chai.expect

const redis = require('../src/redis')

describe("redis connection check", () => {
	it("should return true if redis is pingable", async () => {
		//const value = await redis.testRedisConnection();
		//expect(html).to.be.not.empty;
	})
})